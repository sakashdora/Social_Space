{
  "name": "veil-social",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd backend && npx prisma generate && cd ../frontend && npm run build",
    "vercel-build": "cd backend && npx prisma generate && cd ../frontend && npm run build",
    "prisma:generate": "cd backend && npx prisma generate"
  }
}
